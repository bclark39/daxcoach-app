{
  "categories": [
    {
      "id": "filter-context",
      "title": "Filter Context",
      "patterns": [
        {
          "id": "calculate-basics",
          "title": "CALCULATE Basics",
          "description": "Understanding how CALCULATE modifies filter context to produce different results depending on the filters applied.",
          "example": "Total Sales FC = CALCULATE([Total Sales], 'Date'[Year] = 2024)"
        }
      ]
    },
    {
      "id": "time-intelligence",
      "title": "Time Intelligence",
      "patterns": [
        {
          "id": "yoy-sales",
          "title": "Year-over-Year Sales",
          "description": "Compare sales across years using SAMEPERIODLASTYEAR to evaluate performance trends.",
          "example": "YoY Sales = CALCULATE([Total Sales], SAMEPERIODLASTYEAR('Date'[Date]))"
        }
      ]
    },
    {
      "id": "iterator-functions",
      "title": "Iterator Functions",
      "patterns": [
        {
          "id": "dynamic-measures",
          "title": "Dynamic Measures",
          "description": "Using iterators like SUMX to build dynamic calculations that evaluate row-by-row.",
          "example": "Dynamic Total = SUMX(VALUES('Product'[Category]), [Total Sales])"
        }
      ]
    },
    {
      "id": "row-context",
      "title": "Row Context",
      "patterns": [
        {
          "id": "rls-bridge",
          "title": "RLS with Bridge Tables",
          "description": "Using row context and LOOKUPVALUE to enforce row-level security through a bridge table.",
          "example": "RLS Filter = LOOKUPVALUE('Bridge'[UserID], 'Bridge'[Email], USERPRINCIPALNAME())"
        }
      ]
    }
  ]
}

